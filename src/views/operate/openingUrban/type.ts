
export interface tableItemProps {
    id: string | number
}

export interface tableDataItem {
  id?: string | null;
  city: string | null;
  code: string | null;
  lon: string | null;
  lat: string | null;
}